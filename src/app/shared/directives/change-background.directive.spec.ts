import { ChangeBackgroundDirective } from './change-background.directive';

describe('ChangeBackgroundDirective', () => {
    it('should create an instance', () => {
        const directive = new ChangeBackgroundDirective();
        expect(directive).toBeTruthy();
    });
});
