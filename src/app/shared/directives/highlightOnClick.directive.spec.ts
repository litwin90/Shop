import { HighlightOnClickDirective } from './highlightOnClick.directive';

describe('HighlightOnClickDirective', () => {
    it('should create an instance', () => {
        const directive = new HighlightOnClickDirective();
        expect(directive).toBeTruthy();
    });
});
